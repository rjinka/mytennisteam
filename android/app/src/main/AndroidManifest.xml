<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:remove="android:dataExtractionRules"
        android:icon="@mipmap/my_tennis_team_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_tennis_team_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyTennisTeam"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Link Intent Filter -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Production Link -->
                <data android:scheme="https" />
                <data android:host="mytennisteam.com" />

                <!-- 2. Production Link with 'www' (Best Practice) -->
                <data android:scheme="https" />
                <data android:host="www.mytennisteam.com" />

                <!-- 3. Local Development Link (for Emulator) -->
                <data android:scheme="http" />
                <data android:host="10.0.2.2" />
            </intent-filter>

        </activity>
        <activity
            android:name=".HomeActivity" />
    </application>

</manifest>
